# ReapX · المراقبة

مراقبة احتياطية من خارج Cloudflare (GitHub Actions في مستودع عامّ · مجاني بلا حدّ للدقائق).
الجدولة عند الدقائق 3،8،13…58 من كل ساعة (بعيدًا عن الدقيقة 0 المزدحمة) · وجدولة GitHub «أفضل جهد» قد تتأخّر أو تُسقط تشغيلًا،
فهذه مراقبة ثانية بجانب خدمة مراقبة مخصّصة لا بديل عنها.

| الفحص | الرابط | المطلوب |
|---|---|---|
| الخدمة + الدورة الخلفية | https://reapx.thetasterspage.workers.dev/health?strict=1 | 200 و `"ok":true` |
| لوحة المالك | https://reapx-panel.pages.dev/login.html | 200 |
| صفحة الزبون | https://reapx-qr.pages.dev/pager | 200 |
| تطبيق الشاشة | https://reapx-app.pages.dev/ | 200 |

عند السقوط يصل بريد «Run failed» من GitHub. لا أسرار في هذا المستودع.

`keepalive.yml` يكتب commit في `heartbeat.txt` يومي 1 و15 من كل شهر: GitHub يُطفئ الجدولة بعد ٦٠ يومًا بلا نشاط في المستودع،
وتشغيل workflow أو تفعيله لا يُعدّ نشاطًا · الـcommit نشاط حقيقي في المستودع، لكن التوثيق لا ينصّ صراحةً على أن commit من
GITHUB_TOKEN يكفي، فتُراجع حالة الجدولة (active) كل شهر، والمراقبة الأساسية في خدمة مستقلّة.
